{
  "name": "js.dequeueArray",
  "version": "1.0.0",
  "description": "JavaScript - DequeueArray Data Structure\r Build an JS array with Dequeue Array functionality",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=build/dist.js --target=chrome58",
    "dev": "ts-node src/index.ts",
    "test": "jest --coverage",
    "test:watch": "jest --coverage --watch" 
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "esbuild": "^0.8.39",
    "jest": "^26.6.3",
    "release-it": "^14.2.2",
    "ts-jest": "^26.5.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
